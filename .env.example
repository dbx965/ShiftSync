# Copy this file to .env in the shiftsync (React) folder
# Only needed if your backend runs on a different host or port

# Default is http://localhost:8000 — uncomment and change if needed:
# REACT_APP_API_URL=http://localhost:8000
