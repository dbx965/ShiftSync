I have a React frontend called ShiftSync and a FastAPI backend connected to Supabase. Create a POST endpoint called /students in FastAPI that saves name, email, availability, and shift_interest into the existing Supabase table called students. Use Pydantic models for validation. Then update my React Availability screen so that when the user submits the form, the data is sent to http://127.0.0.1:8000/students using fetch(). After successful submission, navigate to the confirmation screen and display a success message. Keep all existing UI styling unchanged.
Now create a GET endpoint in FastAPI called /students that retrieves all student records from Supabase. Then build a React component called StudentsList that fetches and displays all saved students in a table showing their name, email, availability, and shift interest.
My React app is showing Unable to connect to the server when submitting the availability form. The FastAPI backend is running on port 8000 but the frontend can't reach it. Fix the CORS settings in FastAPI to allow requests from my React app running on localhost:3000 and make sure the fetch URL is correct.
My Supabase URL and API key are currently hardcoded in main.py. Move them to a .env file and use python-dotenv to load them securely. Also create a .env.example file showing the required variables without exposing real values. Make sure .env is added to .gitignore so it is never committed to GitHub.
I want to deploy my ShiftSync app. Deploy the FastAPI backend to Render and the React frontend to Vercel. Create a requirements.txt for the backend. Set up environment variables on both platforms for the Supabase credentials and the backend URL. Update CORS in FastAPI to allow requests from the live Vercel domain.
